import React from 'react'

export default function Sponsors() {
    //For adding payment
    function addPayment(){
        // payment code block
        console.log(clicked);
    }
  return (
    <button onClick={addPayment()}>click</button>
  )
}
